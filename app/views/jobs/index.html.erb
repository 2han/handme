<div class="jobMt">

<h2>仕事一覧</h2>

<div class="col-xs-6 col-sm-8">

<table class="table table-hover .table-bordered">
  <thead>
    <tr>
      <th>報酬額</th>
      <th>依頼者名</th>
      <th>イベント名</th>
      <th>作業内容</th>
      <th>いつ？</th>
      <th>どこで？</th>
    </tr>
  </thead>

  <tbody>
    <% @jobs.each do |job| %>
      <tr>
        <td><%= job.fee %></td>
        <td><%= link_to job.user.name, user_path(job.user) %></td>
        <td><%= job.title %></td>
        <td><%= job.introduction %></td>
        <td><%= job.deadline %></td>
        <td><%= job.place %></td>
        <td class="btn btnMore btn-sm"><%= link_to 'もっと見る', job %></td>
      </tr>
    <% end %>
  </tbody>
</table>

</div>
</div>

<%= will_paginate @jobs %>



