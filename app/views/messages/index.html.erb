<div class="myy">

<table class="mesPT">
  <thead>
    <tr>
      <th>From</th>
      <th>To</th>
      <th>Text</th>
      <th>jobtitle</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @messages.each do |message| %>
      <tr>
        <td><%= message.from_user.name %></td>
        <td><%= message.to_user.name %></td>
        <td><%= message.text %></td>
        <td><%= link_to 'Destroy', message, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<div class="ML"><%= link_to 'New Message', new_message_path %></div>

</div>